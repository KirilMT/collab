# COLLAB Runtime — Production and Development Requirements

## Production Dependencies
# (Used in deployed collab package and live environments)

# Core Supabase client for file lock management
supabase==2.31.0
httpx>=0.28.1

# Process monitoring and system utilities
psutil>=5.9.8

# Desktop notifications (cross-platform)
plyer>=2.1.0

# Environment configuration
python-dotenv>=1.2.2

# Development-only test stub (DO NOT include in wheel)
# pytest-supabase-mock (to be created in Phase 1)
